{
  "name": "photo-book",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "html-includes --src src --dest dist",
    "compile:min": "html-includes --src src --dest dist --minify true caseSensitive=true removeComments=true removeTagWhitespace=true",
    "compile:watch": "html-includes --src src --dest dist --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/s-show/photo-book.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/s-show/photo-book/issues"
  },
  "homepage": "https://github.com/s-show/photo-book#readme",
  "devDependencies": {
    "html-includes": "^5.0.0"
  }
}
